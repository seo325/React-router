import * as React from 'react';
import { Routes, Route, useParams } from 'react-router-dom';

export default function Profile(props){
    // const { id } = useParams();
    // console.log(useParams());
    // console.log(props)
    return <div>#번째 포스트</div>;
    
  
}